# ============================================
# Variables de Entorno - Inmobiliaria Crescendolls
# ============================================
# Copia este contenido a un archivo llamado .env en la raíz del proyecto
# y ajusta los valores según necesites

# Configuración de Base de Datos
DB_HOST=db
DB_USER=inmobiliaria_user
DB_PASS=inmobiliaria_pass
DB_NAME=eq6inmobiliaria

# MySQL Root Password
MYSQL_ROOT_PASSWORD=rootpassword

# Puertos de los Servicios
WEB_PORT=8080
DB_PORT=3306
ADMINER_PORT=8081
